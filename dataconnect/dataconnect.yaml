specVersion: "v1"
serviceId: "first-backend-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "first-backend-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
